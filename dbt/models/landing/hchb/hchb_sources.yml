version: 2

sources:
  - name: HC_LANDING_HCHB
    database: HC_LANDING
    schema: HCHB
    description: "Raw HCHB Bronze tables written by hchb_v12.py generator."
    tables:
      - name: BRANCHES
        identifier: BRANCHES

      - name: PATIENT_INSURANCE
        identifier: PATIENT_INSURANCE

      - name: CLIENT_CONTACT_TYPES
        identifier: CLIENT_CONTACT_TYPES

      - name: EMP
        identifier: EMP

      - name: VISIT
        identifier: VISIT
